%!TEX program = xelatex
%!TEX encoding = UTF-8
\def\ano{2024}
\def\semestre{1}
\def\disciplina{Álgebra 1}
\def\turma{C}
\def\autor{José Antônio O. Freitas}
\def\instituto{MAT-UnB}

\documentclass{beamer}
\usetheme{Madrid}
\usecolortheme{beaver}
% \mode<presentation>
\input{../configuracoes_latex/pacotes}
\input{../configuracoes_latex/ambientes}
\input{../configuracoes_latex/comandos_personalizados}

\title{Retas e Planos no Espaço}
\author[\autor]{\autor}
\institute[\instituto]{\instituto}
\date{}

\begin{document}
    \begin{frame}
        \maketitle
    \end{frame}

    \logo{\includegraphics[scale=.1]{logo-MAT.png}\vspace*{8.5cm}}

    \begin{frame}
        Da Geometria Plana, sabemos que todo plano $\pi$ contém pelo menos três pontos não colineares, digamos $A$, $B$ e $C$. Assim considere os vetores
        \begin{align*}
            \vec{u} = \vec{AB}\\
            \vec{v} = \vec{AC}.
        \end{align*}
        Como $\vec{u}$ e $\vec{v}$ não são paralelos, então podemos considerar o vetor $\vec{u}\times\vec{v}\ne \vec{0}$.
    \end{frame}

    \begin{frame}
        \begin{figure}[!h]
            \centering
            \caption{Equação do plano}\label{definicao-plano}
            \hspace*{-3cm}
            \includegraphics{equacao-plano.pdf}
        \end{figure}
    \end{frame}

    \begin{frame}
        Assim, na Figura \ref{definicao-plano} vemos que um ponto $D$ pertence ao plano $\pi$ se, e somente se, $\vec{u}\times\vec{v} \perp \vec{AD}$. Logo um plano $\pi$ é um conjunto de vetores perpendiculares a um dado vetor.

        Fixe então um ponto $P_0$ em $\real^3$ e $\vec{v}\ne \vec{0}$ um vetor. Passando por $P_0$ existe um \'unico plano $\pi$ perpendicular ao vetor $\vec{v}$. Assim um ponto $P$ do espaço pertence ao plano $\pi$ se, e somente se, $\vec{P_0P} \perp \vec{v}$, isto é,
        \begin{equation}\label{equacaoplanovetorial}
            \inner{P_0P}{v} = 0.
        \end{equation}
    \end{frame}

    \begin{frame}
        Sejam $\vec{v} = (a,b,c)$ um vetor, $P_0(x_0, y_0, z_0)$ e $P(x, y, z)$ pontos em $\real^3$. Logo $\vec{P_0P} = (x - x_0, y - y_0, z - z_0)$ e então da equação \eqref{equacaoplanovetorial} obtemos
        \begin{equation}\label{equacaoplano}
            (x - x_0)a + (y - y_0)b + (z - z_0)c = 0.
        \end{equation}

        A equação \eqref{equacaoplano} é chamada de \textbf{equação cartesiana do plano} $\pi$. O vetor $\vec{v}$ é chamado de \textbf{vetor normal} ao plano $\pi$.\index{Plano!Equação Paramétrica}\index{Plano!Vetor Normal}
    \end{frame}

    \begin{frame}
        \begin{exemplos}
            Encontre a equação do plano $\pi$ nas seguintes situações:
            \begin{enumerate}[label=({\roman*})]
                \item O vetor normal é $\vec{v} = (1,2,-1)$ e o ponto de $\pi$ é $P_0(1,3,-1)$.
                \item O vetor normal é $\vec{v} = (0,0,1)$ e $P_0(0,0,0)$.
                \item Passando pelos pontos $A(3,1,-2)$, $B(5,2,1)$ e $C(2,0,2)$.
            \end{enumerate}
        \end{exemplos}
    \end{frame}

    \begin{frame}
        Dada uma equação da forma
            \[
                ax + by + cz + d = 0,
            \]
        onde $a$, $b$ e $c$ não são simultaneamente nulos, existe um plano $\pi$ representado por esta equação? A resposta é afirmativa. Suponha que $a \ne 0$. Tome $\vec{v} = (a,b,c) \ne 0$ e $P_0(-d/a,0,0)$. A equação do plano com vetor normal $\vec{v}$ e passando por $P_0$ é dada por
            \begin{align*}
                &\inner{P_0P}{v} = 0\\
                &\langle\left(x + \dfrac{d}{a},y,z\right), (a,b,c)\rangle = 0\\
                &ax + by + cz + d = 0.
            \end{align*}
    \end{frame}

    \begin{frame}
        Portanto qualquer equação da forma $ax + by + cz + d = 0$, com $a$, $b$ e $c$ não simultaneamente nulos define um plano.

        Sejam $\pi_1$ e $\pi_2$ dois planos dados por
            \begin{align*}
                \pi_1 : a_1x + b_1y + c_1z + d_1 = 0\\
                \pi_2 : a_2x + b_2y + c_2z + d_2 = 0.
            \end{align*}
        onde os vetores normais de $\pi_1$ e $\pi_2$ são, respectivamente,
            \begin{align*}
                \vec{v_1} = (a_1, b_1, c_1)\\
                \vec{v_2} = (a_2, b_2, c_2).
            \end{align*}
        Os planos $\pi_1$ e $\pi_2$ são \textbf{paralelos} se, e somente se, $\vec{v_1} \varparallel\vec{v_2}$. Para que $\pi_1$ e $\pi_2$ sejam iguais devemos ter $\vec{v_1}\varparallel\vec{v_2}$  e dados $P_1\in\pi_1$ e $P_2\in\pi_2$ o vetor $\vec{P_1P_2}$ deve ser tal que $\vec{P_1P_2}\perp\vec{v_1}$ ou $\vec{P_1P_2}\perp\vec{v_2}$.
    \end{frame}

    \begin{frame}
        \begin{exemplo}
            Dados os seguintes planos, determinar quais são iguais ou paralelos dois a dois:
            \begin{center}
                \begin{tabular}{cc}
                    $\pi_1 : 2x - y + 3z = 0$ &
                    $\pi_2 : x - y - 3 = 0$\\
                    $\pi_3 : y - 2x - 3z = 2$ & $\pi_4 : 2x - 2y = 6$\\
                    $\pi_5 : x = 0$.
                \end{tabular}
            \end{center}
        \end{exemplo}
    \end{frame}

    \begin{frame}
        Seja $\vec{v}$ um vetor e $A$ um ponto. Sabemos que a reta $r$ passando por $A$ e paralela ao vetor $\vec{v}$ tem equação
        \begin{equation}
            \vec{AP} = t\vec{v}
        \end{equation}
        onde $t \in \real$ e $P$ é um ponto qualquer de $r$. Sendo $A$ um ponto em $\real^3$ e $\vec{v}$ um vetor de $\real^3$ temos
        \begin{align*}
            A = (x_0, y_0, z_0)\\
            \vec{v} = (a, b, c).
        \end{align*}
    \end{frame}

    \begin{frame}
        Daí um ponto $P(x, y, z)$ pertence \`a reta $r$ se, e somente se,
        \[
            \vec{AP} = (x - x_0, y - y_0, z - z_0) = t(a, b, c).
        \]
        Logo $P(x, y, z)$ pertence \`a reta $r$, se e s\'o se, $P(x,y,z)$ satisfaz as equações
        \begin{equation}\label{equacoesparametricasretaespaco}
            \begin{cases}
                x = x_0 + ta\\
                y = y_0 + tb\\
                z = z_0 + tc
            \end{cases}; \quad t \in \real.
        \end{equation}
        Tais equações são chamadas de \textbf{equações paramétricas} da reta $r$. O vetor $\vec{v}$ é chamado de \textbf{vetor diretor} de $r$.\index{Reta!Equações Paramétricas}\index{Reta!Vetor Diretor}
    \end{frame}

    \begin{frame}
        \begin{exemplo}
            As equações paramétricas da reta $r$ passando por $P_0(-3,3/2,4)$ e paralela ao vetor $\vec{v} = (-6,1,4)$ são
            \[
                \begin{cases}
                    x = -3 - 6t\\
                    y = 3/2 + t\\
                    z = 4 + 4t
                \end{cases},\quad t \in \real.
            \]
        \end{exemplo}
    \end{frame}

    \begin{frame}
        De forma análoga ao casa do plano, se $\vec{v} = (a,b,c)$ é tal que $abc \ne 0$, então podemos reescrever \eqref{equacoesparametricasretaespaco} como
        \[
            \dfrac{x - x_0}{a} = \dfrac{y - y_0}{b} = \dfrac{z - z_0}{c}
        \]
        que são chamadas de \textbf{equações na forma simétrica} da reta $r$.\index{Reta!Equações Simétricas}
   \end{frame}

   \begin{frame}
        Considere duas retas $r$ e $s$ de equações
        \[
            r: \begin{cases}
                x = x_0 + ta\\
                y = y_0 + tb\\
                z = z_0 + tc
            \end{cases}; \quad s: \begin{cases}
                x = x_1 + \alpha d\\
                y = y_1 + \alpha e\\
                z = z_1 + \alpha f
            \end{cases}; t,\ \alpha \in \real.
        \]
        Os vetores diretores de $r$ e $s$ são $\vec{u} = (a,b,c)$ e $\vec{v} = (d,e,f)$ respectivamente.
   \end{frame}

   \begin{frame}
        \begin{enumerate}
            \item[i)] Se $\vec{u}\varparallel\vec{v}$, então $r$ e $s$ são paralelas ou coincidentes. Elas serão coincidentes se, e somente se, possuírem algum ponto em comum.\index{Retas!Paralelas}
        \end{enumerate}
   \end{frame}

   \begin{frame}
        \begin{figure}[h]
            \centering
            \caption{Retas Paralelas}
            \includegraphics[scale=0.7]{retas-paralelas-espaco.pdf}
        \end{figure}
   \end{frame}

   \begin{frame}
        \begin{enumerate}
            \item[ii)] Se os vetores diretores não são paralelos, temos duas possibilidades. Considere os pontos $P_0(x_0,y_0,z_0) \in r$ e $P_1(x_1,y_1,z_1) \in s$.
            \begin{enumerate}
                \item[1)] Se $\vec{P_1P_2}$, $\vec{u}$ e $\vec{v}$ estão no mesmo plano, isto é, $\langle\vec{P_1P_2}, \vec{u}\times\vec{v}\rangle = 0$ então $r$ e $s$ são concorrentes.
            \end{enumerate}
        \end{enumerate}
   \end{frame}
   
   \begin{frame}
        \begin{figure}[h]
            \centering
            \caption{Retas Concorrentes}
            \includegraphics[scale=0.85]{retas-concorrentes-espaco.pdf}
        \end{figure}
   \end{frame}
    
   \begin{frame}
        \begin{enumerate}
            \item[2)] Se $\vec{P_1P_2}$, $\vec{u}$ e $\vec{v}$ estão planos diferentes, isto é, $\langle\vec{P_1P_2}, \vec{u}\times\vec{v}\rangle \ne 0$ então $r$ e $s$ são chamadas de  \textbf{retas reversas}.\index{Retas! Reversas}
        \end{enumerate}
   \end{frame}
    
   \begin{frame}
        \begin{figure}[h]
            \centering
            \caption{Retas Reversas}
            \vspace{-1cm}
            \includegraphics[scale=0.85]{retas-reversas-espaco.pdf}
        \end{figure}
   \end{frame}
   \begin{frame}
        \begin{exemplos}
            Determine se os seguintes pares de retas são paralelas, concorrentes ou reversas:
            \begin{enumerate}[label=({\roman*})]
                \item $r: \dfrac{x - 2}{2} = y + 3 = \dfrac{z - 2}{3}$; $s:\dfrac{x}{4} = \dfrac{y+3}{2} = \dfrac{z-2}{6}$
                \item
                \[
                    r: \begin{cases}
                        x = 1 + 2t\\
                        y = 1 + 2t\\
                        z = 1 + t
                    \end{cases},\quad s: \begin{cases}
                        x = s\\
                        y = s\\
                        z = 0
                    \end{cases}
                \]
                \item
                \[
                    r: \begin{cases}
                        x = 1 + 2t\\
                        y = 2 + t\\
                        z = -1 + 2t
                    \end{cases},\quad s: \begin{cases}
                        x = 2 + s\\
                        y = 1 + s\\
                        z = -1 + 2s
                    \end{cases}
                \]
           \end{enumerate}
        \end{exemplos}
   \end{frame}
    
   \begin{frame}
        Dados três planos $\pi_1$, $\pi_2$ e $\pi_3$ de equações
        \begin{align*}
            \pi_1 : a_1x + b_1y + c_1z + d_1 = 0\\
            \pi_2 : a_2x + b_2y + c_2z + d_2 = 0\\
            \pi_3 : a_3x + b_3y + c_3z + d_3 = 0.
        \end{align*}
   \end{frame}
    
   \begin{frame}
        A interseção entre eles será constituída pelos pontos cujas coordenadas sejam as soluções do sistema forma pelas equações de $\pi_1$, $\pi_2$ e $\pi_3$ simultaneamente, ou seja, devem ser soluções do sistema
        \[
            \begin{cases}
                a_1x + b_1y + c_1z + d_1 = 0\\
                a_2x + b_2y + c_2z + d_2 = 0\\
                a_3x + b_3y + c_3z + d_3 = 0.
            \end{cases}
        \]
   \end{frame}
    
   \begin{frame}
        \begin{figure}[h]
            \centering
            \caption{$\pi_1\cap\pi_2\cap\pi_3 = \{P\}$}
            \includegraphics[scale=0.5]{intersecao-tres-planos.pdf}
       \end{figure}
   \end{frame}
    
   \begin{frame}
        \begin{figure}[h]
            \caption{$\pi_1\cap\pi_2\cap\pi_3 = \emptyset$}
            \centering
            \includegraphics[scale=0.7]{intersecao-tres-planos-dois-a-dois.pdf}
       \end{figure}
   \end{frame}
    
   \begin{frame}
        \begin{figure}[h]
            \centering
            \caption{$\pi_1\cap\pi_2\cap\pi_3 = \emptyset$}
            {\centering\includegraphics{tres-planos-paralelos.pdf}}
        \end{figure}
   \end{frame}
    
   \begin{frame}
        \begin{exemplos}
            Encontre a interseção entre os planos:
            \begin{enumerate}[label=({\roman*})]
               \item $\pi_1 : x = y$, $\pi_2 : 2x - z = 0$ e $\pi_3 : 3x + 2y + z + 3 = 0$
               \item $\pi_1 : x - 2y + 3z = 2$, $\pi_2 : 3x + y - 2z = 1$ e $\pi_3 : 5x - 3y + 4z = 4$
               \item $\pi_1 : 2x + y = 1$, $\pi_2 : x - y + z = 0$
            \end{enumerate}
        \end{exemplos}
   \end{frame}
    
   \begin{frame}
        Para encontrar a interseção de uma reta $r$ com um plano $\pi$, basta resolver o sistema formado pelas equações de $r$ e de $\pi$. Se o sistema tiver uma \'unica solução $P_0(x_0, y_0, z_0)$, então $r \cap \pi = \{P_0\}$. Se o sistema for indeterminado então $r\subset \pi$ e se o sistema for impossível então $r\cap\pi = \emptyset$ e $r \varparallel \pi$.
   \end{frame}
    
   \begin{frame}
       \begin{figure}[h]
            \centering
            \caption{$r\cap\pi = \{P_0\}$}
            \includegraphics{intersecao-reta-plano.pdf}
       \end{figure}
   \end{frame}
    
   \begin{frame}
        \begin{figure}[h]
            \centering
            \caption{$r\cap\pi= \emptyset$}
            \includegraphics{intersecao-reta-plano-vazia.pdf}
       \end{figure}
   \end{frame}
    
   \begin{frame}
        \begin{figure}[h]
            \centering
            \caption{$r \subset \pi$}
            \includegraphics{reta-contida-no-plano.pdf}
       \end{figure}
   \end{frame}
    
   \begin{frame}
        \begin{exemplos}
            Encontre a interseção entre a reta $r$ e o plano $\pi$ nos seguintes casos:
            \begin{enumerate}[label=({\roman*})]
               \item $r: x = t + 1$, $y = 1$, $z = 1$ e $\pi : 2x - y = 3$.
               \item
                \[
                    r: \begin{cases}
                        2x - y + z = 0\\
                        x + y = 1
                    \end{cases};
                \]
                $\pi : x - 2y + z = 5$.
               \item
                \[
                    r: \begin{cases}
                        2x - y + z = 0\\
                        x + y = 1
                    \end{cases};
                \]
                $\pi : x - y + 3z = -1$.
           \end{enumerate}
        \end{exemplos}
   \end{frame}
    
   \begin{frame}
       Seja $P_1(x_1,y_1,z_1)$ um ponto e $\pi$ um plano de equação $ax + by + cz + d = 0$.
   \end{frame}
    
   \begin{frame}
        \begin{figure}[!h]
            \centering
            \caption{Distância entre ponto e plano}
            \includegraphics[scale=0.7]{distancia-ponto-plano.pdf}
        \end{figure}
   \end{frame}
    
   \begin{frame}
        Seja $r$ a reta que contém o ponto $P_1$ é é perpendicular ao plano $\pi$. Denote por $P_2(x_2,y_2,z_2)$ a interseção de $\pi$ com $r$. O ponto $P_2$ é chamado de \textbf{projeção ortogonal} de $P_1$ sobre $\pi$. A norma do vetor $\vec{P_1P_2}$ será a distância de $P_1$ a $\pi$, isto é,
        \[
            d(P_1,\pi) = \norm{\vec{P_1P_2}}.
        \]
   \end{frame}
    
   \begin{frame}
        Seja $\vec{u} = (a,b,c)$ o vetor normal de $\pi$. Como $\vec{u}\varparallel\vec{P_1P_2}$, existe $t \in \real$ tal que
        \[
            \vec{P_1P_2} = t\vec{u}.
        \]
        Logo
        \begin{equation}\label{equacaodistanciapontoplano}
            d(P_1,P_2) = \norm{t(a,b,c)} = |t|\sqrt{a^2 + b^2 + c^2}.
        \end{equation}
   \end{frame}
    
   \begin{frame}
        Por outro lado, se $P(x_2, y_2, z_2)$, então
        \[
            \vec{P_1P_2} = (x_2 - x_1, y_2 - y_1, z_2 - z_1) = t(a,b,c)
        \]
        e como $P_2 \in \pi$ devemos ter
        \[
            a(x_1 + ta) + b(y_1 + tb) + c(z_1 + tc) + d = 0
        \]
        isto é,
        \begin{equation}\label{equacaoauxiliardistanciapontoplano}
            t = -\dfrac{ax_1 + by_1 + cz_1 + d_1}{a^2 + b^2 + c^2}.
        \end{equation}
   \end{frame}
    
   \begin{frame}
        Substituindo \eqref{equacaoauxiliardistanciapontoplano} em \eqref{equacaodistanciapontoplano} obtemos
        \[
            d(P_1,\pi) = \left|\dfrac{ax_1 + by_1 + cz_1 + d_1}{a^2 + b^2 + c^2}\sqrt{a^2 + b^2 + c^2}\right|
        \]
        portanto
        \[
            d(P_1,\pi) = \dfrac{|ax_1 + by_1 + cz_1 + d_1|}{\sqrt{a^2 + b^2 + c^2}}.
        \]
   \end{frame}
    
   \begin{frame}
        Um outro modo de determinar essa distância é o seguinte: seja $P_0 \in \pi$. Então a distância de $P_1$ a $\pi$ é dada pela norma da projeção ortogonal do vetor $\vec{P_0P_1}$ na direção do vetor normal do plano $\vec{u} = (a,b,c)$. Assim
        \[
            d(P_1,\pi) = \norm{\proj_{\vec{u}}\vec{P_0P_1}} = \dfrac{|\langle\vec{P_0P_1},\vec{u}\rangle|}{\norm{\vec{u}}}.
        \]
   \end{frame}
    
   \begin{frame}
        \begin{figure}[!h]
            \centering
            \caption{Distância entre ponto e plano}
            \includegraphics{distancia-ponto-plano-vetores.pdf}
        \end{figure}
   \end{frame}
    
   \begin{frame}
        \begin{exemplos}
            \begin{enumerate}[label=({\roman*})]
                \item A distância entre o plano $\pi: 2x + y - z = 4$ e o ponto $P(1,1,1)$ será
                \[
                    d(P,\pi) = \dfrac{|2 + 1 - 1 - 4|}{\sqrt{4 + 1 + 1}} = \dfrac{2}{\sqrt{6}}.
                \]
                \item Calcule a distância entre $\pi_1 : 2x - 3y + z - 2 = 0$ e $\pi_2 : 4x - 6y + 2z - 5 = 0$.
           \end{enumerate}
        \end{exemplos}
   \end{frame}
    
   \begin{frame}
        Seja $P$ um ponto e $r$ uma reta.
        \begin{figure}[h]
            \centering
            \caption{Distância entre ponto e reta}
            \vspace{-3cm}
            \includegraphics{distancia-ponto-reta.pdf}
       \end{figure}
   \end{frame}
    
   \begin{frame}
        Para determinar $d(P,r)$ primeiro construímos um plano $\pi$ passando por $P$ e com vetor normal paralelo ao vetor diretor de $r$. Assim
        \[
            d(P,r) = d(P,P_1)
        \]
        onde $P_1 \in \pi\cap r$.
   \end{frame}
    
   \begin{frame}
        \begin{exemplo}
            Determinar a distância do ponto $P(1,2,-1)$ \`a reta
            \[
                r: \begin{cases}
                    x = 1 + 2t\\
                    y = 5 - t\\
                    z = -2 + 3t.
                \end{cases}
            \]
       \end{exemplo}
   \end{frame}
    
   \begin{frame}
        Sejam $r$ e $s$ retas. Se $r \varparallel s$ então a distância entre $r$ e $s$ é dada pela distância entre um ponto de $r$ e a reta $s$, ou o contrário. Suponha então que $r$ e $s$ são concorrentes ou reversas. %Por um ponto $P$ de $s$ tracemos uma reta $s'$ paralela a $r$.

        Estas duas retas definem dois planos paralelos (que podem ser coincidentes, no caso em que as retas são concorrentes). Um plano é o que contém $r$ e é paralelo a $s$, que vamos chamar de $\pi_1$. O outro contém $s$ e é paralelo a $r$, que chamaremos de $\pi_2$.
   \end{frame}
    
   \begin{frame}
        \begin{figure}[!h]
            \centering
            \caption{Distância entre retas reversas}
            \includegraphics{distancia-retas-reversas-alternativa.pdf}
        \end{figure}
   \end{frame}
    
   \begin{frame}
        Sejam $\vec{u}$ e $\vec{v}$ os vetores diretores de $r$ e $s$, respectivamente. Então o vetor $\vec{w} = \vec{u}\times\vec{v}$ é normal aos planos $\pi_1$ e $\pi_2$. Assim
        \[
            d(r,s) = d(\pi_1,\pi_2).
        \]
        Mas
        \[
            d(\pi_1,\pi_2) = d(\pi_1, Q) = \dfrac{|\langle\vec{PQ},\vec{w}\rangle|}{\norm{\vec{w}}} = \dfrac{|\langle\vec{PQ},\vec{u}\times\vec{v}\rangle|}{\norm{\vec{u}\times\vec{v}}}
        \]
        onde $P \in r$ e $Q \in s$. Portanto,
        \[
            d(r,s) = d(\pi_1, Q) = \dfrac{|\langle\vec{PQ},\vec{w}\rangle|}{\norm{\vec{w}}} = \dfrac{|\langle\vec{PQ},\vec{u}\times\vec{v}\rangle|}{\norm{\vec{u}\times\vec{v}}}.
        \]

        Se $r \cap s \ne \emptyset$, então $d(r,s) = 0$ pois $\vec{PQ}$, $\vec{u}$ e $\vec{v}$ estão no mesmo plano.
   \end{frame}
    
   \begin{frame}
        \begin{exemplo}
            Determine a distância entre as retas
            \[
                r: \begin{cases}
                    x = 2 + t\\
                    y = 1 - 3t\\
                    z = 1 + 2t
                \end{cases} \quad s: \begin{cases}
                    x = -5 + 4s\\
                    y = 6 - 5s\\
                    z = 4 + 3s.
                \end{cases}
            \]
       \end{exemplo}
   \end{frame}
\end{document}
